<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.OnePassword" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:statusBarColor">@color/neutral_dark</item>
        <item name="colorAccent">@color/white</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.OnePassword" parent="Base.Theme.OnePassword" />
    <style name="button_theme">
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/button_shape</item>
        <item name="android:fontFamily">@font/poppins</item>
        <item name="android:textStyle">bold</item>
        <item name="android:padding">@dimen/_8sdp</item>
    </style>
</resources>